#ifndef STACK_FUNCTIONS
#define	STACK_FUNCTIONS
#include "structDeclarations.h"

_Stack* PopStack(_Stack* head, _Tree* root);
int Push(_Stack* head, _Stack* newDir);
int Add(_Tree* root, _Stack* head);

#endif

